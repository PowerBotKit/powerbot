{
  "name": "@powerbotkit/examples",
  "version": "0.0.0",
  "description": "examples of using powerbotkit",
  "main": "index.ts",
  "scripts": {
    "build": "tsc --build",
    "start-distributor": "tsc --build && node dist/distributor.js",
    "start-consumer": "tsc --build && node dist/consumer.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^4.0.3"
  }
}
