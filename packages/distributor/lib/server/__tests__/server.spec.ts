import { IBotServer } from '..';

describe('server', () => {
	// beforeAll(async() => {
	// 	server = await createDistributorServer();
	// });

	it('should create', () => {
		// expect(server).toBeTruthy();
	});
});
